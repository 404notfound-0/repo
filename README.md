# 国内大模型 API 价格对比

这个仓库用于托管一个静态 HTML 页面，适合直接部署到 GitHub Pages。

## 文件

- `index.html`: 页面入口
- `.nojekyll`: 禁用 Jekyll 处理，按纯静态文件发布

## 访问方式

启用 GitHub Pages 后，页面通常可通过以下地址访问：

`https://<GitHub 用户名>.github.io/<仓库名>/`
